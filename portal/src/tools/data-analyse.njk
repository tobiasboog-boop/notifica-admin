---
layout: false
title: Data Analyse - Notifica
permalink: /tools/data-analyse/
robots: noindex, nofollow
---
<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Analyse - Notifica</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html, body { height: 100%; overflow: hidden; }
        .header {
            background: white;
            border-bottom: 1px solid #e5e7eb;
            padding: 12px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 50px;
            position: relative;
            z-index: 1000;
        }
        .header h1 {
            font-family: 'Inter', -apple-system, sans-serif;
            font-size: 1rem;
            font-weight: 600;
            color: #111827;
        }
        .header a {
            font-family: 'Inter', -apple-system, sans-serif;
            font-size: 0.85rem;
            color: #6b7280;
            text-decoration: none;
        }
        .header a:hover { color: #16136F; }
        iframe {
            width: 100%;
            height: calc(100vh - 50px);
            border: none;
        }
        .placeholder {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: calc(100vh - 50px);
            background: #f9fafb;
            font-family: 'Inter', -apple-system, sans-serif;
        }
        .placeholder .icon { font-size: 4rem; margin-bottom: 20px; }
        .placeholder h2 { font-size: 1.5rem; color: #111827; margin-bottom: 12px; }
        .placeholder p { font-size: 0.95rem; color: #6b7280; max-width: 500px; text-align: center; line-height: 1.6; margin-bottom: 24px; }
        .placeholder .btn {
            display: inline-block;
            background: #16136F;
            color: white;
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            margin-bottom: 16px;
        }
        .placeholder .btn:hover { background: #0f0d4d; }
        .placeholder .note { font-size: 0.8rem; color: #9ca3af; margin-top: 16px; }
    </style>
</head>
<body>
    <header class="header">
        <a href="{{ '/admin/' | url }}">← Terug naar Admin</a>
        <h1>Data Analyse</h1>
    </header>

    <div class="placeholder" id="placeholder">
        <span class="icon">🔍</span>
        <h2>Data Analyse Dashboard</h2>
        <p>Analyseer klantdata exports uit Syntess. Ontdek patronen, meldingen en kritieke informatie in de geëxporteerde data.</p>
        <a href="https://boostinsite-mrifxryhuhwa2gtdja5i5j.streamlit.app/" target="_blank" class="btn">Open Dashboard</a>
        <span class="note">Dashboard opent in een nieuw tabblad op Streamlit Cloud</span>
    </div>

    <iframe id="dashboard" src="https://boostinsite-mrifxryhuhwa2gtdja5i5j.streamlit.app/?embed=true" allow="clipboard-write" style="display:none;"></iframe>

    <script>
        const iframe = document.getElementById('dashboard');
        const placeholder = document.getElementById('placeholder');

        iframe.onload = function() {
            try {
                placeholder.style.display = 'none';
                iframe.style.display = 'block';
            } catch(e) {
                placeholder.style.display = 'flex';
                iframe.style.display = 'none';
            }
        };

        iframe.onerror = function() {
            placeholder.style.display = 'flex';
            iframe.style.display = 'none';
        };

        // Timeout fallback - if iframe doesn't respond in 5 seconds, show placeholder
        setTimeout(function() {
            if (iframe.style.display !== 'block') {
                placeholder.style.display = 'flex';
            }
        }, 5000);
    </script>
</body>
</html>
